# OSE — v0.1.4-LIB — تقليم جراحي بعد أول قياس حقيقي من المترجم

التاريخ: 2026-09-17 · المدخل: خطأ CE10117 عند **100,417** مقابل حد **100,256** ⇒ تجاوز **161 token**.

---

## 1. المعايرة الحقيقية — أهم نتيجة في هذه الجولة

| | |
|---|---:|
| v0.1.2 (بلا مكتبة) | 105,227 |
| v0.1.3-LIB | **100,417** (مقاس) |
| الفرق المقاس | **4,810 compiled** |
| ما أزلته من المؤشر | **2,138 token مصدري** |
| **النسبة الحقيقية** | **2.250 compiled لكل token مصدري** |

تقديري السابق كان 2.67 (معايَر من حزمة R7)، أي **أعلى بـ19%**. لهذا أسقطتُ 99,530 وجاء 100,417. النسبة الجديدة مقاسة على **نفس القاعدة البرمجية** وبفارق كبير (2,138 token)، فهي الأدق المتاحة، واعتمدتها هنا.

---

## 2. الرافعة المستخدمة: tokens المكتبة مجانية

بما أن كود المكتبة خارج ميزانية المؤشر، فإن نقل دالة يوفّر:

```
الربح الصافي = حجم جسم الدالة − (2 × عدد مواضع النداء)
```

أي أن المرشح المثالي هو **جسم كبير ومواضع نداء قليلة** — عكس ما كان مجديًا قبل وجود المكتبة. لهذا لم أنقل `s7_priceText` (39 نداءً) ولا `hasBit` (18 نداءً): نقلها **يزيد** الحجم.

### فحص السلامة الحاسم
فرزت كل دوال المؤشر آليًا على معيار **انعدام الاعتماد على أي series global**. اكتشفت أثناء الفرز خللًا في أداتي: كانت تعتبر كل اسم بأحرف كبيرة ثابتًا، بينما `VPS_*` **قيم series** لا ثوابت (`color VPS_Q0 = s7c_card`). بعد التصحيح استُبعدت كل دوال `vp_*Tint/*Bg`، لأن نقلها كان سيتطلب تمرير ألوان متغيرة كوسائط ويقلب الربح إلى خسارة.

تحققت من أن كل ثابت مُضمَّن هو **`const` حقيقي**: `S7_NA`, `VP_NA`, `BIT_VWAP/PDH/PDL/ORH/ORL`, `STATIC_MASK_MAX`, `QB_Q1..Q5`.

---

## 3. ما نُقل — أربع دوال، صفر اعتماد على series

| الدالة | جسم | نداءات | ربح صافٍ | اعتماداتها |
|---|---:|---:|---:|---|
| `rvolMedian` | 150 | 2 | **146** | لا شيء إطلاقًا |
| `s7_zoneText` | 137 | 1 | **135** | ثوابت + `hasBit` |
| `vp_zoneText` | 123 | 6 | **111** | ثوابت + `hasBit` |
| `s7_qCount` → `qCount` | 120 | 1 | **104** | ثوابت + `hasBit` |
| **المجموع** | | | **506 مصدري** | |

- `hasBit` **لم تُنقل**: نُسخت داخل المكتبة (مجانية هناك)، وبقيت نسخة المؤشر كما هي لمواضع ندائها الـ18. هذا يمنع خسارة 36 token.
- `rvolMedian` تُرتّب مصفوفة المتصل **في مكانها**؛ المصفوفات في Pine مراجع، فالتمرير إلى المكتبة يحافظ على التعديل الموضعي حرفيًا.
- `s7_qCount` كانت تُعيد tuple؛ صارت تُعيد `QCount` UDT (نمط مُثبت بالفعل في v1). الجسم مطابق بايتيًا؛ تغيّر سطر التوقيع وسطر الإرجاع فقط.

---

## 4. النتيجة

| | |
|---|---:|
| أُزيل من المؤشر | **506 token مصدري** |
| بالنسبة المقاسة 2.25 | **≈ 1,138 compiled** |
| **الإسقاط** | **100,417 − 1,138 ≈ 99,278** |
| **الهامش تحت الحد** | **≈ 978** |

حساسية: عند نسبة 2.0 ⇒ 99,405 (هامش 851) · عند 2.5 ⇒ 99,152 (هامش 1,104). **في كل النطاق يعبر الحد بهامش يفوق المطلوب (300–500) بمرتين تقريبًا.**

---

## 5. جدول التحقق

| # | الفحص | النتيجة | الدليل |
|---|---|---|---|
| 1 | ثبات بايتي للأجسام المنقولة | **PASS** | diff آلي: rvolMedian / s7_zoneText / vp_zoneText = IDENTICAL؛ qCount الجسم IDENTICAL |
| 2 | كل ثابت مُضمَّن هو `const` حقيقي | **PASS** | تحقق مباشر من 13 إعلانًا |
| 3 | لا اعتماد على series في المنقول | **PASS** | فرز آلي بعد تصحيح تصنيف `VPS_*` |
| 4 | حدود التعديل | **PASS** | 16 كتلة diff، كلها متوقعة (4 حذوفات + 10 مواضع نداء + banner + رقم الاستيراد) |
| 5 | لا تعريف متبقٍ ولا نداء يتيم | **PASS** | لا تعريف محلي باقٍ؛ لا بادئة مزدوجة |
| 6 | `s7_qPass/qAppl/qOk` تُحلّ | **PASS** | مُعرَّفة 6162–6164، مستهلكة 7681 |
| 7 | plots (53) / inputs (95) / requests (3+1) / alert (1) | **PASS** | الأعداد مطابقة |
| 8 | عناوين وترتيب الـ52 export | **PASS** | diff مطابق |
| 9 | Stage 6/7 semantics، Profile/Delta/R4 | **PASS** | لم تُمس؛ R4 خارج النطاق كليًا |
| 10 | **Compile / Load** | **NOT RUN** | لا وصول للمنصة |

---

## 6. خطوات النشر

1. Pine Editor ← الصق `OSE-RF1-COMPANION-LIB-v2-TEST-ONLY.pine` فوق المكتبة الحالية.
2. **Publish script ← Update** (وليس Publish new) ⇒ ستصبح **الإصدار 2**.
3. في المؤشر: سطر الاستيراد صار `.../OSE_RF1_COMPANION/2`. استبدل `YOUR_TV_USERNAME` بحسابك. **إن أعطتك المنصة رقم إصدار مختلفًا، ضعه بدل الرقم 2.**
4. Save ← Add to chart.

> المؤشر v0.1.4 **يتطلب المكتبة v2**. لا تُجمّعه على v1: `rvolMedian`/`s7_zoneText`/`vp_zoneText`/`qCount` غير موجودة فيها.

---

## 7. ما لم أفعله ولماذا

- **لم أنقل** `v4_matrixTruth` (601) ولا `v4_collectReferences` (415) رغم ضخامتهما: تعتمدان على 22 و30 قيمة series على التوالي؛ تمريرها كوسائط يلغي الربح.
- **لم أطبّق** رفع `array.size()` المتكرر داخل النطاق (47 token): في `v4_trimEvents` تُحذف عناصر من `records` داخل الحلقة، فرفع `array.size` **يغيّر السلوك**. رفضته رغم أنه كان في قائمتي السابقة.
- **لم أمسّ** R4 ولا Profile/Delta ولا أي طلب أو plot أو alert، كما طلبت.

---

## 8. الحكم

**التجاوز لم يُثبَت أنه عولج — لكن الإسقاط هذه المرة مبني على نسبة مقاسة لا مقدَّرة.**

الفجوة كانت 161 وأُزيل ما يقابل ~1,138، أي **سبعة أضعاف التجاوز**. ومع ذلك **TRADINGVIEW COMPILE/LOAD NOT RUN** ولا أدّعي رقمًا. أعِد لي رقم المترجم: إن نجح، أحتاج تأكيد Load وسلامة العرض؛ إن بقي فوق الحد، الرافعة التالية جاهزة (`fillDateList` 107 + `dnumFromStr` 95 + `vp_riskReason` 76 + `s7_rejectText` 76 + `closeLocOf` 73 + `s7_deltaText` 64 ≈ 491 مصدري ≈ 1,105 compiled إضافية بنفس المعيار).
